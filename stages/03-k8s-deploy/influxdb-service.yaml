apiVersion: v1
kind: Service
metadata:
  name: influxdb
  namespace: {{NAMESPACE}}
spec:
  type: ClusterIP
  selector:
    app: influxdb
  ports:
    - name: http
      port: 8086
      targetPort: 8086

